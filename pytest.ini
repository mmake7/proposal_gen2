[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers

markers =
    unit: 단위 테스트 (빠른 실행, 외부 의존성 없음)
    integration: 통합 테스트 (모듈 간 연동)
    api: API 엔드포인트 테스트
    slow: 느린 테스트 (대용량 PDF 등)
